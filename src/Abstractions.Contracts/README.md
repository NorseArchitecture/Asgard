# Norse.Abstractions.Contracts

Norse declared law: the `NorsePrincipal` identity surface, `Population` value type, published domain event interfaces, and the `IAccountApi` cross-realm read contract. The single assembly other product contexts reference from `Norse.Abstractions`.

Part of the [Norse Architecture](https://github.com/NorseArchitecture) platform.
